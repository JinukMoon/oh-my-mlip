# build_status: candidate
# candidate-reason: openequivariance is sdist-only (builds against nvcc/torch)
# oh-my-mlip env recipe: NequIP (nequip)
#
# Pins mirror the validated internal setup:
#   python 3.11.13 / torch 2.9.1+cu128.
# install.sh build-from-recipe is the PRIMARY install path today. (conda-pack
# tarballs are planned but not yet published; do not assume any are hosted.)
#
#   - torch is pinned to the CUDA 12.8 build (cu128) from the official PyTorch
#     wheel index; the host NVIDIA driver must satisfy the cu128 min-driver floor.
#   - cuda-nvcc 12.8.* is included so the D3 dispersion kernel can compile on
#     first run (matching the torch CUDA minor) AND so openequivariance can build
#     its CUDA extensions.
#   - nequip is pinned to the validated version; e3nn is its top-level dep.
#   - openequivariance ships sdist-only and compiles against nvcc + the installed
#     torch at install time; this is the reason the build is marked candidate.
#   - catbench is installed LAST, with --no-deps, to avoid its dependency pins
#     downgrading torch's CUDA libraries.
name: nequip
channels:
  - conda-forge
  - nvidia
dependencies:
  - python=3.11.13
  - pip
  # CUDA 12.8 toolchain (provides nvcc for D3 and for openequivariance's build)
  - cuda-nvcc=12.8.*
  - ase
  - pip:
      - --extra-index-url https://download.pytorch.org/whl/cu128
      - torch==2.9.1+cu128
      - nequip==0.15.0
      - e3nn
      - openequivariance==0.4.1
      # catbench LAST and isolated; see header note.
      - --no-deps
      - catbench==1.1.2
